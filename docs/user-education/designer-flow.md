
# designer
Designer is a chat, UI assisted by a series of Subway that Health design designer the product owner that they lead. QA lead to have a conversation and create the low-level design document.



## Flow

[[designer-flow-diagram]]

## Login
Once you arrive at the designer login page, you will login with the help of the GitHub ID that is already available with you

If you are logging for the first time, you might have to give access to designer application for your particular login

## Prerequisites 

As part of the designer work, we are expecting the user to provide a Kitha token which has access to GCP Finacle Park. This is required because we will be doing a clone and creating the full request with the identity of the user rather than a centralised login.

## Context

Design designer behind the scenes will create a workspace spaceDesign designer behind the scenes will create a workspace space This workspace is pretty much like the feature branch that you might create after doing a get clothes on your local Machine in order for us to create the workspace. We need the following basic details from you, which repository that you would be working on. What is the jira number that you would be working on? We will use the username from your Geeta login and responding Github token, which has writ access to that particular 

There are also utility tools provided for you to view the context that is currently being operated on and also to edit it if required
For example, if you want to work on a different Jira, you could use the edit context to create workspace with a different jira

## Workspace creation
Once you have provided the context data, designer will suggest That you proceed with workspace creation behind the scenes, we will be triggering a Jenkins pipeline, which will create a clone of the repository that you are working on That you proceed with workspace creation behind the scenes, we will be triggering a Jenkins pipeline, which will create a clone of the repository that you are working on With a feature branch created placed on the Jaya number that you have provided all the Code changes are in this case, the LED changes would be present in that particular feature branch, and the full request will be finally raised from that Future branch to developer

## Sub agents

We have a subagent for each of the LED sections
These patients are specialised for you to have a conversation and get the desired LED section as an output
They have a very strong understanding of what is the output that needs to be generated and And also, what each of the elements of that particular output Really mean